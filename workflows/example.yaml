name: "Simple Example - Generate Output"
models:
  deep-seek:
    type: "ollama"
    model: "deepseek-r1:7b"
  llama:
    type: "ollama"
    model: "llama2:1b"
task:
  name: "Generate Output"
  steps:
    - name: "Generate Output"
      model: "deep-seek"
      id: "step-1"
      parameters:
        prompt: "What is 1+1 equal?"